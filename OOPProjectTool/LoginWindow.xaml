<Window x:Class="OOPProjectTool.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="356" Width="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Benutzer anmelden"
                   FontSize="20"
                   FontWeight="Bold"
                   Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1"
                   Text="Benutzername"
                   FontWeight="Bold"/>

        <ComboBox x:Name="CmbBenutzer"
                  Grid.Row="2"
                  Margin="0,5,0,15"
                  DisplayMemberPath="Name"/>

        <Border Grid.Row="3" BorderBrush="LightGray" BorderThickness="1" Padding="10">
            <StackPanel>
                <TextBlock Text="Verfügbare Test-Benutzer:"
                           FontWeight="Bold"
                           Margin="0,0,0,8"/>
                <TextBlock Text="• Hans (Projektleiter)"/>
                <TextBlock Text="• Lisa (Mitarbeiter)"/>
                <TextBlock Text="• Max (Mitarbeiter)"/>
                <TextBlock Text="Hinweis: Für den Prototyp ist kein Passwort erforderlich."
                           Margin="0,12,0,0"
                           FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <Button Grid.Row="4"
                Content="Login"
                Height="35"
                Margin="0,15,0,0"
                Click="Login_Click"/>
    </Grid>
</Window>